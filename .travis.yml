version: ~> 1.0

import:
  - silverstripe/silverstripe-travis-shared:config/provision/standard-jobs-range-behat-npm.yml

env:
  global:
    # Change to "4.10.x-dev || " once the 4.10 branch in silverstripe/assets has been created
    - REQUIRE_RECIPE="4.x-dev || "
    - BEHAT_SUITE=sharedraftcontent
